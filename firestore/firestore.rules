rules_version = '2';

// 기본 deny 골격 (M0-07). 컬렉션별 실제 규칙은 M1+ 에서 추가한다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
